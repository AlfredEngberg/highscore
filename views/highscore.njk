{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
  <h1>{{ title }}</h1>
  <ul>
    <h2>Top 10 list</h2>
    <ul>
      {% for score in scores %}
      <li>{{ score.score }}, {{ game.name }}, {{ user.name }}</li>
      {% endfor %}
    </ul>
</main>
{% endblock %}