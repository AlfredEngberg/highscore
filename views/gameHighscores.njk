{% extends "layout.njk" %}

{% block content %}
<main class="wrapper">
    <h2>Top 10 list for {{ game.name }}</h2>

    <ul>
        {% for score in scores %}
        <li>{{ score.score }} | {{ score.game }} | {{ score.username }}</li>
        {% endfor %}
    </ul>
</main>
{% endblock %}